let arr = [2, 4, 6, 8, 10];
console.log(arr.length);

for (let i = 0; i < arr.length; i++) {
    console.log(arr[i]);
}


//попробовала использовать обратные кавычки как оператор интерполяции
let arr = [2, 4, 6, 8, 10];
console.log(`Длина массива равна ${arr.length}`);

for (let i = 0; i < arr.length; i++) {
    console.log(`Элементы массива: ${arr[i]}`);
  
}
